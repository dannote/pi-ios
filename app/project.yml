name: BunTest
options:
  bundleIdPrefix: dev.bun
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.0"

targets:
  BunTest:
    type: application
    platform: iOS
    sources:
      - path: PiTerminal/BunTest
        excludes:
          - BridgingHeader.h
    settings:
      base:
        INFOPLIST_FILE: PiTerminal/BunTest/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: dev.bun.test
        SWIFT_VERSION: "6.0"
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGNING_ALLOWED: "NO"
        SWIFT_OBJC_BRIDGING_HEADER: PiTerminal/BunTest/BridgingHeader.h
        HEADER_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PROJECT_DIR)/../vendor/bun/include"
        LIBRARY_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PROJECT_DIR)/../vendor/bun/lib"
        OTHER_LDFLAGS:
          # Bun static libraries
          - "-lbun"
          - "-lJavaScriptCore"
          - "-lWTF"
          - "-lbmalloc"
          - "-lcrypto"
          - "-lssl"
          - "-ldecrepit"
          - "-lbrotlicommon"
          - "-lbrotlidec"
          - "-lbrotlienc"
          - "-lcares"
          - "-lhwy"
          - "-ldeflate"
          - "-llolhtml"
          - "-lls-hpack"
          - "-lz"
          - "-larchive"
          - "-lhdr_histogram_static"
          - "-lzstd"
          # mimalloc object file
          - "$(PROJECT_DIR)/../vendor/bun/lib/mimalloc.o"
          # System libraries
          - "-licucore"
          - "-lresolv"
          - "-lc++"
          - "-lobjc"
          # Frameworks
          - "-framework Metal"
          - "-framework CoreText"
          - "-framework CoreGraphics"
          - "-framework CoreFoundation"
          - "-framework Foundation"
          - "-framework QuartzCore"
          - "-framework IOSurface"
          - "-framework CoreVideo"
          - "-framework UIKit"
          - "-framework Security"
